/*
 * sbt
 * Copyright 2026, Scala Center, and the sbt contributors.
 * Licensed under Apache License 2.0 (see LICENSE).
 */

package sf
class Left { def value: Int = new UpJava().value().toInt + 1 + new UpScala().value + new api.JavaBase().value() + new api.ScalaBase().value + new api.sf.OnlyJava().value() }
